@model HomePageViewModel
@{
    ViewData["Title"] = "Ana Sayfa";
}

<div class="site-shell">
    <header class="topbar">
        <div class="topbar__inner section-container">
            <nav class="topbar__nav" aria-label="Ana menu">
                <a class="topbar__link topbar__link--active" asp-controller="Products" asp-action="Index">Urunler</a>
                <a class="topbar__link" href="#marka-hikayesi">Hakkimizda</a>
                <a class="topbar__link" href="#bulten">Iletisim</a>
            </nav>
            <a class="topbar__brand" asp-controller="Home" asp-action="Index">FUWANS</a>
            <div class="topbar__actions" aria-label="Hizli islemler">
                <button class="icon-button" type="button" aria-label="Hesabim">
                    <span class="material-symbols-outlined">person</span>
                </button>
                <button class="icon-button" type="button" aria-label="Favoriler">
                    <span class="material-symbols-outlined">favorite</span>
                </button>
                <button class="icon-button" type="button" aria-label="Sepet">
                    <span class="material-symbols-outlined">shopping_bag</span>
                </button>
            </div>
        </div>
    </header>

    <section class="hero">
        <div class="hero__media">
            <img src="@Model.Hero.ImageUrl" alt="@Model.Hero.Title" />
            <div class="hero__overlay"></div>
        </div>
        <div class="hero__content section-container">
            <span class="eyebrow">@Model.Hero.Eyebrow</span>
            <h1>@Model.Hero.Title</h1>
            <div class="hero__actions">
                <a class="button button--solid" asp-controller="Products" asp-action="Index">@Model.Hero.PrimaryActionText</a>
                <a class="button button--ghost" asp-controller="Products" asp-action="Index">@Model.Hero.SecondaryActionText</a>
            </div>
        </div>
    </section>

    <section class="feature-strip">
        <div class="section-container feature-strip__items">
            @foreach (var feature in Model.Features)
            {
                <div class="feature-strip__item">
                    <span class="material-symbols-outlined">@feature.IconName</span>
                    <span>@feature.Title</span>
                </div>
            }
        </div>
    </section>

    <section class="section-container section-spacing" id="one-cikanlar">
        <div class="section-heading">
            <div>
                <h2>Seckin Parcalar</h2>
                <p>@Model.FeaturedProductsDescription</p>
            </div>
            <a class="section-link" asp-controller="Products" asp-action="Index">Tumunu Gor</a>
        </div>

        @if (Model.FeaturedProducts.Count > 0)
        {
            var featuredProduct = Model.FeaturedProducts[0];
            var secondaryProducts = Model.FeaturedProducts.Skip(1).ToList();

            <div class="product-grid">
                <article class="product-card product-card--featured">
                    <a class="product-card__link" asp-controller="Products" asp-action="Details" asp-route-slug="@featuredProduct.Slug">
                        <div class="product-card__image product-card__image--tall">
                            <img src="@featuredProduct.PrimaryImageUrl" alt="@featuredProduct.Name" />
                        </div>
                        <div class="product-card__header">
                            <h3>@featuredProduct.Name</h3>
                            <span>@featuredProduct.PriceText</span>
                        </div>
                        <p class="product-card__meta">@featuredProduct.ShortDescription</p>
                    </a>
                </article>

                <div class="product-grid__stack">
                    @foreach (var product in secondaryProducts)
                    {
                        <article class="product-card">
                            <a class="product-card__link" asp-controller="Products" asp-action="Details" asp-route-slug="@product.Slug">
                                <div class="product-card__image product-card__image--square">
                                    <img src="@product.PrimaryImageUrl" alt="@product.Name" />
                                </div>
                                <div class="product-card__header">
                                    <h3>@product.Name</h3>
                                    <span>@product.PriceText</span>
                                </div>
                                <p class="product-card__meta">@product.ShortDescription</p>
                            </a>
                        </article>
                    }
                </div>
            </div>
        }
    </section>

    <section class="collection-band section-spacing" id="kategori-vitrini">
        <div class="section-container category-grid">
            @foreach (var category in Model.HighlightCategories)
            {
                <article class="category-card">
                    <img src="@category.HighlightImageUrl" alt="@category.Name" />
                    <div class="category-card__overlay">
                        <h3>@category.Name</h3>
                        <a asp-controller="Products" asp-action="Index">
                            @category.CallToActionText
                            <span class="material-symbols-outlined">arrow_forward</span>
                        </a>
                    </div>
                </article>
            }
        </div>
    </section>

    <section class="section-container section-spacing story-grid" id="marka-hikayesi">
        <div class="story-grid__media">
            <div class="story-grid__frame">
                <img src="@Model.BrandStory.ImageUrl" alt="@Model.BrandStory.Title" />
            </div>
        </div>
        <div class="story-grid__content">
            <span class="eyebrow eyebrow--dark">@Model.BrandStory.Eyebrow</span>
            <h2>@Model.BrandStory.Title</h2>
            <div class="story-grid__text">
                @foreach (var paragraph in Model.BrandStory.Paragraphs)
                {
                    <p>@paragraph</p>
                }
            </div>
            <a class="section-link" href="#bulten">@Model.BrandStory.ActionText</a>
        </div>
    </section>

    <section class="newsletter section-spacing" id="bulten">
        <div class="newsletter__inner section-container section-container--narrow">
            <h2>Ic Cembere Katilin</h2>
            <p>@Model.NewsletterDescription</p>

            @if (TempData["SubscriptionMessage"] is string message)
            {
                <div class="newsletter__message">@message</div>
            }

            <form asp-action="Subscribe" asp-controller="Home" method="post" class="newsletter__form">
                @Html.AntiForgeryToken()
                <div class="newsletter__field">
                    <label asp-for="SubscriptionEmail">E-posta Adresi</label>
                    <input asp-for="SubscriptionEmail" type="email" placeholder="ORNEK@EPOSTA.COM" />
                </div>
                <button class="button button--solid" type="submit">Abone Ol</button>
            </form>
        </div>
    </section>

    <footer class="footer">
        <div class="section-container footer__grid">
            <div>
                <div class="footer__brand">FUWANS</div>
                <p class="footer__summary">@Model.FooterSummary</p>
            </div>
            <div class="footer__links">
                <div>
                    <span>Kesfet</span>
                    <a asp-controller="Products" asp-action="Index">Urunler</a>
                    <a href="#kategori-vitrini">Koleksiyonlar</a>
                    <a href="#marka-hikayesi">Ozel Uretim</a>
                </div>
                <div>
                    <span>Kurumsal</span>
                    <a href="#marka-hikayesi">Hakkimizda</a>
                    <a href="#bulten">Surdurulebilirlik</a>
                    <a asp-controller="Home" asp-action="Privacy">Yasal</a>
                </div>
            </div>
            <div class="footer__meta">
                <div class="footer__icons">
                    <a href="#" aria-label="Dil">
                        <span class="material-symbols-outlined">language</span>
                    </a>
                    <a href="#" aria-label="Paylas">
                        <span class="material-symbols-outlined">share</span>
                    </a>
                </div>
                <div>© 2026 FUWANS. MODERN MIRASI YENIDEN TANIMLIYORUZ.</div>
            </div>
        </div>
    </footer>
</div>
